//
//  StudyFile.swift
//  IOS-OC
//
//  Created by 北川 on 2025/2/28.
//

import Foundation

// MARK: For studying Object-C (konwledge).

/**
 
 类：关键字定义（@interface @end为一个范围），内部可以定义类属性和方法声明（但是不能直接使用需要对类声明方法进行实现）。
 
 实例方法：方法组成为：- （返回类型）方法名：参数 {}
 
 类方法：方法组成为：+ （返回类型）方法名：参数 {}
 
 类实现：关键字定义（@implementation @end为一个范围），对实现的类进行方法实现。
 
 基础数据结构：整型（Int），浮点型（Float），双浮点（Double），布尔值（Bool），字符串（NSString），
 
 RunLoop：
 
 RunTime：
 
 GCD：
 
 常用组件：
 
 基础网络知识：
 
 
 
 */
